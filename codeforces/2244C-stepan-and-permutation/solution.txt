#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
ll gcdll(ll a, ll b){
    return b ? gcdll(b, a % b) : a;
}
void solve() {
    int n, x, y;
    cin >> n >> x >> y;
 
    vector<int> arr(n);
    for (int i = 0; i < n; i++) {
        cin >> arr[i];
    }
 
    int count = 0;
    int lcm = gcdll(x, y);
 
    for (int i = 0; i < n; i++) {
        if (abs(arr[i] - (i + 1)) % lcm == 0) {
        } 
        else {
            count++;
        }
    }
    if (count == 0) {
        cout << "YES\n";
    } else {
        cout << "NO\n";
    }
}
 
int main() {
    int T;
    cin >> T;
    while (T--) {
        solve();
    }
    return 0;
}